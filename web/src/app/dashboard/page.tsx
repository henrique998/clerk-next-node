import { Button } from './button'

export default function Dashboard() {
  return (
    <main className="min-h-screen w-full flex items-center justify-center gap-4">
      <h1>Dashboard</h1>

      <Button />
    </main>
  )
}